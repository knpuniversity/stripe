{% extends 'base.html.twig' %}

{% block body %}
    <h1>{{ product.name }}</h1>

    <strong>{{ product.price }}</strong>

    <img width="100" src="{{ asset('uploads/'~product.imageFilename) }}" />

    <p>
        {{ product.description }}
    </p>
{% endblock %}

